const hello = (name) => {
    console.log("Hello!", name);
}
hello('Sam Njenga');

hi = (name) => {
    console.log("Hi!", name);
}

hi('Nice Programmer');


function dev(name) {
    console.log(name)
}
dev('Bill Gates')

const printName = function (firstName, lastName) {
    const name = firstName + lastName;
    console.log(name);
}
printName('Kelvin ', 'Kimaru')

const city = function fullCity(firstName, lastName) {
    const city = firstName + lastName;
    console.log(city);
}
city('Ruiru ', 'Kiambu')

